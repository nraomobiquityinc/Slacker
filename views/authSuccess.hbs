<div class="col-md-8 row">
  <h4>Thank you for authorizing us, {{userName}}!</h4>
</div>

<div class="col-md-8 row">
  <h4>We can now run your queued commands for you.</h4>
</div>

<div class="row">
  <div class="col-md-8">
    <table class="table table-striped table-bordered">
      <tr class="titleRow">
        <td>Run</td>
        <td>Action name</td>
        <td>Time Requested</td>
      </tr>
      <input type="hidden" id="userId" value="{{userId}}"></input>
      {{#each actionsList}}
        <tr>
          <td>
            <input type="checkbox" name="{{@index}}-command" id="{{@index}}-command" checked/>
          </td>
          <td>
            <span class="cmd">{{this.data.command.name}}</span>
          </td>
          <td>{{this.timeStamp}}</td>
        </tr>
      {{/each}}
    </table>
    <div class="alert alert-info" role="alert">
      You may choose which actions you wish to run. Unchecked items will be deleted without being run.
      <p>
    </div>
    <button type="button" id="submitButton" class="btn btn btn-success">Run!</button>
  </div>
</div>

<script id="actionsDone" type="text/x-handlebars-template">
  <div class="col-md-8 row">
    <h4>We're running your selected actions... You may now close this window.</h4>
  </div>
  <div class="col-md-8 alert alert-success" role="alert">
    Remember, now that you've authorized us, in the future, you can run actions requiring authorization directly from Slack.
  </div>
</script>